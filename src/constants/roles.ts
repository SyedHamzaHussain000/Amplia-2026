export enum UserRole {
    USER = "user",
    SUB_ADMIN = "subAdmin",
    SUPER_ADMIN = "superAdmin"
}

export enum SubAdminStatus {
    ACTIVE = "active",
    INACTIVE = "inactive"
}

export enum BookingStatus {
    NEW = "new",
    SCHEDULED = "scheduled",
    ACTIVE = "active",
    COMPLETED = "completed",
}